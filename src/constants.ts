// Upload image limit is 20MB
export const IMAGE_SIZE_LIMIT = 20 * 1024 * 1024;

// Photos are compressed in the browser before they cross the server boundary.
export const COMPRESSED_IMAGE_SIZE_LIMIT = 2 * 1024 * 1024;

// Upload default folder
export const DEFAULT_FOLDER = "photos";
